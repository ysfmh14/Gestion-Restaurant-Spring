spring:
  datasource:
    url: jdbc:mysql://localhost/Restaurant?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  servlet:
    context-path: /api
  port: 8080
